# Changelog

## 0.5.1 (2026-09-18)

- 模块视图实机验证通过（Spartan7 板 USB 模块：USB-C 座 + 3×TVS + CH9441，
  互联 10 网络 + 对外 8 引脚）。按实机输出修三处显示瑕疵：
  - 第 4 节电源脚明细改按引脚号自然排序（此前为符号原始顺序，USB 座一行为乱序）；
  - 电源脚引脚名与引脚号相同时只写一处（TVS 的 `8 8` → `8`），全图导出同步生效；
  - 文末"模块视图"斜体收尾多余空格修正（避免 Markdown 渲染出原始星号）。

## 0.5.0 (2026-09-18)

- **新增：模块视图——「导出选中器件互联」命令**（第二条菜单命令）。逐功能模块
  开发时不需要整板导出：框选本模块的全部器件（≥2 个，如 MCU + 485 收发器 +
  USB 接口座）后运行，只输出四块内容：
  - 选定器件清单（型号/制造商/封装/引脚数）；
  - **器件间互联网络**：连接 ≥2 个选中器件的信号网络逐行列出，
    `网络: 选中侧连接点 = 连接点`（同一网络），未选中器件以"，外部："标注；
  - **对外信号引脚**（模块边界）：网络只连到本模块一处的引脚 + 模块外去向；
  - 电源/未连接汇总（电源脚明细 + 未连接计数）。
  模块视图只认画布选中（不并入设置位号/自动识别）；电源过滤、紧凑/表格风格、
  电气方向标注等设置与全图导出共用；文件名为 `前缀-module-日期.md`。
- **全图导出联动提示**：「导出连接信息」检测到选中 ≥2 个器件时，完成 toast
  追加提示模块视图命令（避免误把整板导出当模块互联用）。
- 电阻/电容/电感统一显示值（v0.4.2 引入）随包发布。

## 0.4.2 (2026-09-18)

- **电阻/电容/电感统一显示值**（实机反馈：C8 显示完整料号 HGC0402R5106M100NTEJ、
  C9 却显示 470nF，来源不一致且料号对理解电路无信息量）。R/C/L 开头的位号改为
  **值优先**：自定义属性 Value/Comment > 器件名称 > 料号 > 库器件名 > 符号名；
  值缺失时退回原链路，IC（U 开头等）保持料号优先不变。

## 0.4.1 (2026-09-18)

- **修复：同名网络未合并**（v0.4.0 实机输出暴露）：几何法按导线簇重建时，两片互不
  相连但打同名标签的导线（如跨页的 DDR_DQ0、分散的 GND）成为两个独立网络，导致
  核心表对端为空、电源汇总只显示第一簇成员。新增 `mergeNetsByName`——网络名的语义
  就是"同名即同网"，网表/几何两源统一按名合并（成员去重）。
- **新增裸电压形态规则**：`数字[.数字]V[数字]`（1V5、1V0、12V、3.3V…）一律归电源
  （默认名单枚举不到的电压名不再漏进信号网络表；EXTV 等非裸电压形态不受影响）。

## 0.4.0 (2026-09-18)

## 0.4.0 (2026-09-18)

- **新增：几何法网络重建（第三级数据源）**。实机复测确认 Spartan7 板上两个网表接口
  （`sch_Netlist.getNetlist` 与 `sch_ManufactureData.getNetlistFile`）均返回空，v0.3.1
  仍无法获得网络。本版彻底摆脱对网表 API 的依赖：
  - 读取全部导线（`sch_PrimitiveWire.getAll()`：折线坐标 + 导线网络名）；
  - 读取网络标识（电源/地符号，`getAll('netflag')`：位置 + 网络名）与引脚坐标；
  - 并查集聚类：同一导线连通、引脚/标识坐标落在导线段上归入该簇；
  - 簇命名优先级：`sch_Net.getCurrentProjectAllNets()` 全局网络名（按导线 ID）>
    网络标识名（GND/+3V3 等）> 导线自带名 > 自动名 `Net位号_引脚号`；
    一簇多名（疑似短路/跨页重名）拼接命名并在控制台告警；
  - 网表接口仍优先：解析成功就不走几何法；几何法统计（导线/命中/未命中计数）输出
    到控制台，便于判断导线枚举是否覆盖全部图页。
- v0.3.1 的型号清洗（`={Value}` 模板过滤）与多源网表回退保持不变。

## 0.3.1 (2026-09-18)

## 0.3.1 (2026-09-18)

- **修复：实机（Spartan7 板，179 器件）网表读取为空**，导致全部 225 个 FPGA 引脚显示
  "未连接"、所有器件信号列为空。`sch_Netlist.getNetlist('Protel2')`（已废弃接口）在
  部分客户端返回空。新增网表多源回退链：
  1. `sch_Netlist.getNetlist('Protel2')`（字符串或 File/Blob 形态均可解析）；
  2. 为空时自动改用 `sch_ManufactureData.getNetlistFile('sch-conn-export', 'Protel2')`
     （官方推荐替代接口）读取网表文本。
- **空网表醒目告警**：两路网表都为空时，结果弹窗与文档头部均提示"网表读取为空，
  未连接不代表真实电路"，控制台输出网表来源/长度/原文前 300 字符便于反馈排查
  （旧版静默输出空结果，易误当作真实电路）。
- **型号清洗**：装配变量模板占位（如 `={Value}`）不再作为型号输出，自动跳过并
  依次回退 自定义属性 Value/Comment → 库器件名 → 符号名。

## 0.3.0 (2026-09-17)

## 0.3.0 (2026-09-17)

- **移除全部外部交互接口引用，安装不再触发"外部交互权限"提示**：
  - 删除 `sys_FileSystem.saveFile`（客户端静态扫描 bundle，只要引用该类接口就会在安装时提示权限默认禁用）；
  - 导出主路径改为「生成 → 打开预览窗口」：一键复制全文（剪贴板）、下载 .md（窗口内浏览器下载
    blob + a[download]，不经过 EDA 文件接口）、全选文本；全文同时输出到控制台留档；
  - 菜单合并为单命令「导出连接信息（预览 / 复制 / 下载）」，删除原「预览 / 复制」二级命令；
  - verify-pkg 新增回归守卫：bundle 不得出现 sys_FileSystem/WebSocket/XMLHttpRequest 等接口。
- 预览窗口：显示文件名与字符数，内容超长截断时自动禁用下载按钮并提示走复制/控制台。

## 0.2.0 (2026-09-17)

## 0.2.0 (2026-09-17)

- **对端连接点补全引脚号**：`U5.SCL` → `U5.SCL(6)`，核心表可直接回答"MCU 引脚号/引脚名 ↔
  FPGA/外设引脚号/引脚名"的互联问题（引脚名与引脚号相同的无功能名引脚仍省写为 R4.1）。
- **紧凑输出风格（默认）**：核心器件引脚表改用代码块文本行（`引脚号 引脚名 网络 = 对端`），
  去掉表格线与表头 token 开销；原表格风格可在设置切回。
- **网络清单去重**：经过核心器件的网络已在核心表完整展开，网络清单默认只列**不经过核心器件**
  的网络（可切回全量）——信息零丢失，显著压缩上下文占用。
- **可选引脚电气方向**：开启后在核心表引脚名后标注符号方向 [I]/[O]/[IO]/[P]/[G]（默认关）。
- 文档头合并为单行统计 + 记法说明行（首次向 LLM 解释连接点格式）。
- 修复：关闭"忽略电源/地"总开关时，"全成员电源脚网络"规则不应继续生效。

## 0.1.0 (2026-09-17)

## 0.1.0 (2026-09-17)

- 首个版本：整图收集全部图页器件与 Protel2 网表，导出面向大模型阅读的 Markdown 连接文档。
- 核心器件 = 画布选中 ∪ 设置位号（只选引脚自动反查父器件），可选按引脚数自动识别；
- 核心器件引脚表：引脚号/引脚名/网络/对端连接点（位号.引脚名），电源脚与未连接脚（含 No ERC）单独成行；
- 电源过滤三层规则：网络名通配名单（可配）→ 引脚名兜底（VDD/VSS/VCC/GND/VBAT/VREF/EP…）→ 全成员电源脚网络整体归类；总开关可关闭；
- 信号网络总表、其它器件一览（位号/型号/信号连接）、被忽略电源网络汇总三张视图，章节可独立开关；
- 导出 .md 文件（前缀-日期命名）与预览/复制窗口（一键复制全文），全文同时留档控制台；
- 多子部件器件（U1A/U1B）聚合、网表-画布双向容错位号匹配、未匹配连接点诊断输出。
